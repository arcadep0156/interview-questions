name: Auto-Generate Index

on:
  push:
    branches:
      - '**'
    paths:
      - 'data/**/*.json'

jobs:
  generate-index:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Generate index.json
        run: node scripts/generate-index.js
      
      - name: Generate contributors.json
        run: node scripts/generate-contributors.js
      
      - name: Update README with contributors
        run: node scripts/update-readme-contributors.js
      
      - name: Check for changes
        id: check
        run: |
          if git diff --quiet index.json contributors.json README.md; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
      
      - name: Commit and push if changed
        if: steps.check.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.json contributors.json README.md
          git commit -m "chore: auto-update stats and contributors [skip ci]"
          git push
